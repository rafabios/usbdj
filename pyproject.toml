[project]
name = "usb-dj-formatter"
version = "0.1.0"
description = "Formatter portatil para preparar pendrives para CDJ/XDJ no Windows."
requires-python = ">=3.11"
dependencies = []

[project.optional-dependencies]
build = ["pyinstaller>=6.0"]

[tool.unittest]
start-directory = "tests"

